# dev_guard_example

Demonstrates how to use the `dev_guard` Flutter plugin.

Replace `your_project_id` and `YOUR_MASTER_SECRET` in `lib/main.dart` with credentials from [devguard.uk](https://devguard.uk).
